# Server Run IP
HOST_IP='RUN_IP'
# Server Run PORT
HOST_PORT='RUN_PORT'

# 서버 도메인 (QR 생성 시 활용)
SERVER_DOMAIN='https://film.example.com'
# 관리자 이메일 정보 (메인페이지에 표시됨)
ADMIN_EMAIL='example@gmail.com'

# 관리자 비밀번호 (bcrypt 해시)
# python3 -c 'import bcrypt, getpass; print(bcrypt.hashpw(getpass.getpass("Password: ").encode("utf-8"), bcrypt.gensalt()).decode("utf-8"))'
ADMIN_PASSWORD='ADMIN_PAGE_PASSWORD'
# 관리자 페이지 세션 idle 타임아웃 (초)
ADMIN_SESSION_TIMEOUT=300
# 관리자 세션 절대 만료 (초)
ADMIN_SESSION_ABSOLUTE_TIMEOUT=3600

# 로그 파일 경로
LOGFILE_PATH='./src/film.log'

# 세션 암호화 키
# python3 -c 'import secrets; print(secrets.token_hex(32))'
SESSION_SECRET_KEY='SECRET_SESSION_KEY'
CSRF_SECRET_KEY='SECRET_CSRF_KEY(not_used)'
